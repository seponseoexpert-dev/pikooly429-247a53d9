// Stub: legacy per-product/district delivery checker has been removed.
// Delivery is now controlled globally via `useDeliveryPresets`.
const DeliveryChecker = (_props: any) => null;
export default DeliveryChecker;
